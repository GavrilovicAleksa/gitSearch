import { SizeConversionPipe } from './size-conversion.pipe';

describe('SizeConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
